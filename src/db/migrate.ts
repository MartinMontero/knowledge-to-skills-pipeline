// Migration script placeholder
// Database migrations are only needed when DB_URL and DB_TOKEN are configured
// This file allows the build process to complete when no database is available

console.log("Database migrations skipped - no database configuration found");

process.exit(0);
